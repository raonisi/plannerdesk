# PR-154 — 런타임 Smoke

| 명령 | 상태 |
| --- | --- |
| `npm run smoke:public` | 존재 — `BASE_URL` + running server |
| `npm run test:e2e` | **명령 부재** |
| `npm run test:smoke` | **명령 부재** |

신규 스크립트·의존성 추가 없음 (PR154 원칙).

/**
 * PR-COPY-A / PR-PUBLIC-SAFETY-A — re-exports from canonical public copy guard.
 */
export {
  PUBLIC_SURFACE_FORBIDDEN_UI_PHRASES,
  PUBLIC_SURFACE_ROUTE_FILES,
  PUBLIC_EMPTY_CONTENT_UPDATING,
  PUBLIC_PLACEHOLDER_UNAVAILABLE,
} from "@/lib/public/public-copy-guard";
